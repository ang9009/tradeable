// import { createContext } from "react";

// const ChatContext = createContext();

// function ChatContextProvider({ children }) {
//   const INITIAL_STATE = {
//     chatId: "null",
//     user: {},
//   };

//   const chatReducer = {state, action} => {
//     switch(action.type) {
//         case "CHANGE_USER":
//             return {
//                 user: action.payload,
//                 chatId:
//             };

//         default:
//             return state;
//     }
//   };

//   return (
//     <ChatContext.Provider value={currUser}>{children}</ChatContext.Provider>
//   );
// }

// export default ChatContextProvider;
